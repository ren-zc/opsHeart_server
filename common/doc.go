package common

// If there is a proxy between server and agent, it should set agent ip to "X-Forwarded-For".
// And also set server ip to "X-Forwarded-For".
// The proxy should set ip of self to "X-Proxy-IP".
